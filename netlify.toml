[build]
  command = "npm run build"
  publish = "dist/public"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- client/ shared/ package.json package-lock.json vite.config.ts"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
